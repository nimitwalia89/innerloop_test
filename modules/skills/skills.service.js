(function ()
{
    'use strict';

    angular
        .module('helpDesk.skills')
        .factory('skillsService', skillsService);

    /** @ngInject */
    function skillsService($http, $rootScope)
    {
    return {
      
    }
    }
})();
